<h1><%= post.title %></h1>
<p><%= post.content %></p>
<h2>Comments:</h2>
<ul>
    <% post.comments.forEach(comment => { %>
        <li><a href="/comments/<%= comment.id %>"><%= comment.content %></a>
    <% }); %>
</ul>
